#!/usr/bin/env bash

# OpenCode environment configuration
# Note: performance tuning (compaction, model) is done in opencode.json
# This file is kept for any shell-level env vars that may be needed in the future.
